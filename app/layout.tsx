export const metadata = {
  title: 'TopVisas',
  description: 'Immigration Assessment Tool',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}